#!/bin/sh

cat<<EOF > iso/syslinux/syslinux.cfg
default coreos
prompt 1
timeout 15
 
label coreos
  kernel /coreos/vmlinuz
  append initrd=/coreos/cpio.gz state=tmpfs: sshkey="$(cat /id_rsa.pub)"
EOF
